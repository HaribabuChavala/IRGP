apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "google-adk-agent.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "google-adk-agent.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "google-adk-agent.fullname" . }}
    spec:
      containers:
        - name: google-adk-agent
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
          envFrom:
            {{- if .Values.config.enabled }}
            - configMapRef:
                name: {{ include "google-adk-agent.fullname" . }}-config
            {{- end }}
            {{- if .Values.secrets.enabled }}
            - secretRef:
                name: {{ include "google-adk-agent.fullname" . }}-secret
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
