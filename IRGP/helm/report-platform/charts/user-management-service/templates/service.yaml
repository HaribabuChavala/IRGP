apiVersion: v1
kind: Service
metadata:
  name: {{ include "user-management-service.fullname" . }}
spec:
  selector:
    app: {{ include "user-management-service.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
