apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "ai-sql-service.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "ai-sql-service.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "ai-sql-service.fullname" . }}
  template:
    metadata:
      labels:
        app: {{ include "ai-sql-service.fullname" . }}
    spec:
      containers:
        - name: ai-sql-service
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: {{ .Values.service.port }}
          envFrom:
            {{- if .Values.config.enabled }}
            - configMapRef:
                name: {{ include "ai-sql-service.fullname" . }}-config
            {{- end }}
            {{- if .Values.secrets.enabled }}
            - secretRef:
                name: {{ include "ai-sql-service.fullname" . }}-secret
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 10
