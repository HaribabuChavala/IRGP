apiVersion: v1
kind: Service
metadata:
  name: {{ include "report-generation-service.fullname" . }}
spec:
  selector:
    app: {{ include "report-generation-service.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  type: ClusterIP
